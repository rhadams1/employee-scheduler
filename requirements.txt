# Ice Line Employee Scheduler - Dependencies
# Install: pip install -r requirements.txt

flask>=3.0.0
openpyxl>=3.1.0
fpdf2>=2.7.0
python-dotenv>=1.0.0
gunicorn>=21.0.0  # Production WSGI server
playwright>=1.40.0  # Headless Chromium for branded PDF export (FPDF fallback if absent)
